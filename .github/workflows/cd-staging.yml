name: CD Pipeline - Deploy to Kubernetes

on:
  # Trigger when CI pipeline completes successfully on main branch
  workflow_run:
    workflows: ["CI Pipeline (Modular)"]
    types: [completed]
    branches: [main]
  
  # Allow manual trigger for testing
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - prod
      run_k8s_apply:
        description: 'Run kubectl apply/smoke tests from runner'
        required: true
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  KUBERNETES_CONTEXT: docker-desktop
  TARGET_NAMESPACE: staging  # For inception phase, always deploy to staging

jobs:
  # ============================================================================
  # PHASE 1: Verify CI Success
  # ============================================================================
  verify-ci:
    runs-on: ubuntu-latest
    # Only proceed if CI succeeded or manual trigger
    if: |
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
      github.event_name == 'workflow_dispatch'
    outputs:
      should_deploy: ${{ steps.check.outputs.should_deploy }}
      environment: ${{ steps.check.outputs.environment }}
      namespace: ${{ steps.check.outputs.namespace }}
    steps:
      - name: Verify deployment conditions
        id: check
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            ENV="${{ github.event.inputs.environment }}"
            echo "📋 Manual deployment triggered for: ${ENV}"
          else
            ENV="staging"
            echo "🚀 Auto-deployment triggered by CI success"
          fi
          
          if [ "$ENV" == "prod" ]; then
            NAMESPACE="prod"
          else
            NAMESPACE="staging"
          fi
          
          echo "should_deploy=true" >> $GITHUB_OUTPUT
          echo "environment=${ENV}" >> $GITHUB_OUTPUT
          echo "namespace=${NAMESPACE}" >> $GITHUB_OUTPUT
          echo "🎯 Deploying to: ${ENV} (namespace: ${NAMESPACE})"

  # ============================================================================
  # PHASE 2: Prepare Deployment
  # ============================================================================
  prepare-deployment:
    needs: verify-ci
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      git-sha: ${{ steps.git.outputs.sha }}
      repo-owner: ${{ steps.repo.outputs.owner }}
      environment: ${{ needs.verify-ci.outputs.environment }}
      namespace: ${{ needs.verify-ci.outputs.namespace }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version from deployment files
        id: version
        run: |
          # Extract version from deployment YAML files
          DEPLOYMENT_FILE="infra/k8s/${{ needs.verify-ci.outputs.namespace }}/deployments/auth-service/deployment.yaml"
          if [ -f "$DEPLOYMENT_FILE" ]; then
            IMAGE_TAG=$(grep -E "image:.*:v[0-9]" "$DEPLOYMENT_FILE" | head -1 | sed -E 's/.*:v([0-9]+\.[0-9]+\.[0-9]+).*/\1/')
            if [ -n "$IMAGE_TAG" ]; then
              echo "version=${IMAGE_TAG}" >> $GITHUB_OUTPUT
              echo "📊 Version: ${IMAGE_TAG}"
            else
              # Fallback to latest
              echo "version=latest" >> $GITHUB_OUTPUT
              echo "📊 Version: latest (fallback)"
            fi
          else
            echo "version=latest" >> $GITHUB_OUTPUT
            echo "📊 Version: latest (fallback)"
          fi

      - name: Get Git SHA
        id: git
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          echo "sha=$SHORT_SHA" >> $GITHUB_OUTPUT
          echo "🔖 SHA: $SHORT_SHA"

      - name: Get repo owner
        id: repo
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "owner=$OWNER" >> $GITHUB_OUTPUT

  # ============================================================================
  # PHASE 3: Verify Images Exist
  # ============================================================================
  verify-images:
    needs: [verify-ci, prepare-deployment]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        service:
          - { name: "auth", image: "freshharvest-market-auth" }
          - { name: "user", image: "freshharvest-market-user" }
          - { name: "product", image: "freshharvest-market-product" }
          - { name: "order", image: "freshharvest-market-order" }
          - { name: "payment", image: "freshharvest-market-payment" }
          - { name: "gateway", image: "freshharvest-market-gateway" }
          - { name: "frontend", image: "freshharvest-market-frontend" }
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify image exists
        run: |
          VERSION="${{ needs.prepare-deployment.outputs.version }}"
          SHA="${{ needs.prepare-deployment.outputs.git-sha }}"
          OWNER="${{ needs.prepare-deployment.outputs.repo-owner }}"
          
          # Try versioned tag first
          if [ "$VERSION" != "latest" ]; then
            IMAGE_TAG="ghcr.io/${OWNER}/${{ matrix.service.image }}:v${VERSION}"
            echo "Checking for image: ${IMAGE_TAG}"
            if docker manifest inspect "${IMAGE_TAG}" > /dev/null 2>&1; then
              echo "✅ Image found: ${IMAGE_TAG}"
              exit 0
            fi
          fi
          
          # Fallback to latest
          IMAGE_TAG="ghcr.io/${OWNER}/${{ matrix.service.image }}:latest"
          echo "Falling back to: ${IMAGE_TAG}"
          docker manifest inspect "${IMAGE_TAG}" || exit 1
          echo "✅ Image found: ${IMAGE_TAG}"

  # ============================================================================
  # PHASE 4: Deploy to Kubernetes
  # ============================================================================
  deploy-to-kubernetes:
    needs: [prepare-deployment, verify-images]
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_k8s_apply == 'true'
    permissions:
      contents: read
    environment:
      name: ${{ needs.prepare-deployment.outputs.environment }}
      url: ${{ needs.prepare-deployment.outputs.environment == 'prod' && 'http://api.freshharvest-market.local' || 'http://staging.freshharvest-market.local' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubectl context
        run: |
          # For Docker Desktop Kubernetes (local)
          # In production, you would configure cloud provider credentials here
          echo "Configuring kubectl for Docker Desktop..."
          # kubectl config set-context docker-desktop
          # kubectl config use-context docker-desktop

      - name: Verify namespace exists
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          kubectl get namespace "${NAMESPACE}" || \
          kubectl create namespace "${NAMESPACE}"

      - name: Create image pull secret (if needed)
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          # Check if secret already exists
          if ! kubectl get secret ghcr-secret -n "${NAMESPACE}" > /dev/null 2>&1; then
            echo "⚠️ ghcr-secret not found in ${NAMESPACE} namespace"
            echo "Please create it manually:"
            echo "kubectl create secret docker-registry ghcr-secret \\"
            echo "  --docker-server=ghcr.io \\"
            echo "  --docker-username=YOUR_USERNAME \\"
            echo "  --docker-password=YOUR_TOKEN \\"
            echo "  --namespace=${NAMESPACE}"
          else
            echo "✅ Image pull secret exists"
          fi

      - name: Apply Kubernetes manifests
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          ENV="${{ needs.prepare-deployment.outputs.environment }}"
          echo "🚀 Applying Kubernetes manifests to ${ENV} (namespace: ${NAMESPACE})..."
          
          # Apply in order: namespaces, RBAC, configmaps, secrets, deployments
          kubectl apply -f infra/k8s/${NAMESPACE}/namespaces/ || true
          kubectl apply -f infra/k8s/${NAMESPACE}/rbac/
          kubectl apply -f infra/k8s/${NAMESPACE}/configmaps/
          kubectl apply -f infra/k8s/${NAMESPACE}/secrets/ || true
          kubectl apply -f infra/k8s/${NAMESPACE}/deployments/
          kubectl apply -f infra/k8s/${NAMESPACE}/ingress/ || true
          kubectl apply -f infra/k8s/${NAMESPACE}/storage/ || true

      - name: Wait for deployments to be ready
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          echo "⏳ Waiting for all deployments to be ready in ${NAMESPACE}..."
          kubectl wait --for=condition=available --timeout=300s \
            deployment/auth-service \
            deployment/user-service \
            deployment/product-service \
            deployment/order-service \
            deployment/payment-service \
            deployment/gateway \
            deployment/frontend \
            -n "${NAMESPACE}" || true

      - name: Get deployment status
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          ENV="${{ needs.prepare-deployment.outputs.environment }}"
          echo "## 📊 Deployment Status - ${ENV}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          kubectl get deployments -n "${NAMESPACE}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🚀 Pods Status" >> $GITHUB_STEP_SUMMARY
          kubectl get pods -n "${NAMESPACE}" >> $GITHUB_STEP_SUMMARY

  # ============================================================================
  # PHASE 5: Smoke Tests
  # ============================================================================
  smoke-tests:
    needs: [deploy-to-kubernetes]
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_k8s_apply == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubectl context
        run: |
          echo "Configuring kubectl for Docker Desktop..."
          # kubectl config use-context docker-desktop

      - name: Wait for services to be ready
        run: |
          echo "Waiting for services to be ready..."
          sleep 30  # Give services time to start

      - name: Run smoke tests
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          ENV="${{ needs.prepare-deployment.outputs.environment }}"
          echo "🧪 Running smoke tests against ${ENV} environment..."
          
          # Test gateway health endpoint
          GATEWAY_POD=$(kubectl get pods -n "${NAMESPACE}" -l app=gateway -o jsonpath='{.items[0].metadata.name}' || echo "")
          
          if [ -z "$GATEWAY_POD" ]; then
            echo "⚠️ Gateway pod not found, skipping gateway health check"
          else
            echo "Testing gateway health endpoint..."
            kubectl exec -n "${NAMESPACE}" "$GATEWAY_POD" -- \
              curl -f http://localhost:80/api/health || echo "⚠️ Gateway health check failed"
          fi
          
          # Test auth-service health endpoint
          AUTH_POD=$(kubectl get pods -n "${NAMESPACE}" -l app=auth-service -o jsonpath='{.items[0].metadata.name}' || echo "")
          
          if [ -z "$AUTH_POD" ]; then
            echo "⚠️ Auth service pod not found, skipping auth health check"
          else
            echo "Testing auth-service health endpoint..."
            kubectl exec -n "${NAMESPACE}" "$AUTH_POD" -- \
              curl -f http://localhost:80/api/health || echo "⚠️ Auth service health check failed"
          fi
          
          echo "✅ Smoke tests completed"

      - name: Get service endpoints
        run: |
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          echo "## 🔗 Service Endpoints" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          kubectl get services -n "${NAMESPACE}" >> $GITHUB_STEP_SUMMARY

  # ============================================================================
  # PHASE 6: Deployment Summary
  # ============================================================================
  deployment-summary:
    needs: [prepare-deployment, deploy-to-kubernetes, smoke-tests]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Deployment Summary
        run: |
          ENV="${{ needs.prepare-deployment.outputs.environment }}"
          NAMESPACE="${{ needs.prepare-deployment.outputs.namespace }}"
          echo "## 🎉 Deployment to ${ENV^} Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ needs.prepare-deployment.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Git SHA:** ${{ needs.prepare-deployment.outputs.git-sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** ${ENV^}" >> $GITHUB_STEP_SUMMARY
          echo "**Namespace:** ${NAMESPACE}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Deployed Services:" >> $GITHUB_STEP_SUMMARY
          echo "- Auth Service" >> $GITHUB_STEP_SUMMARY
          echo "- User Service" >> $GITHUB_STEP_SUMMARY
          echo "- Product Service" >> $GITHUB_STEP_SUMMARY
          echo "- Order Service" >> $GITHUB_STEP_SUMMARY
          echo "- Payment Service" >> $GITHUB_STEP_SUMMARY
          echo "- Gateway" >> $GITHUB_STEP_SUMMARY
          echo "- Frontend" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Next Steps:" >> $GITHUB_STEP_SUMMARY
          echo "1. Verify services are running: \`kubectl get pods -n ${NAMESPACE}\`" >> $GITHUB_STEP_SUMMARY
          echo "2. Check logs: \`kubectl logs -n ${NAMESPACE} deployment/<service-name>\`" >> $GITHUB_STEP_SUMMARY
          if [ "$ENV" == "prod" ]; then
            echo "3. Access frontend: http://app.freshharvest-market.local" >> $GITHUB_STEP_SUMMARY
            echo "4. Access API: http://api.freshharvest-market.local" >> $GITHUB_STEP_SUMMARY
          else
            echo "3. Access frontend: http://staging.freshharvest-market.local" >> $GITHUB_STEP_SUMMARY
            echo "4. Access API: http://api.staging.freshharvest-market.local" >> $GITHUB_STEP_SUMMARY
          fi
