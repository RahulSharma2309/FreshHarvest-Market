---
# Staging Namespace  
# Purpose: Pre-production testing, mirrors production
# Permissions: QA team and developers (limited)
apiVersion: v1
kind: Namespace
metadata:
  name: staging
  labels:
    environment: staging
    managed-by: freshharvest-market
    cost-center: qa
  annotations:
    description: "Staging environment for final testing before production"
