# User Service Deployment for Production
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: prod
  labels:
    app: user-service
    environment: production
    managed-by: freshharvest-market
spec:
  replicas: 3
  selector:
    matchLabels:
      app: user-service
      environment: production
  template:
    metadata:
      labels:
        app: user-service
        environment: production
        managed-by: freshharvest-market
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      serviceAccountName: user-service-sa
      containers:
      - name: user-service
        image: ghcr.io/rahulsharma2309/freshharvest-market-user:v1.2.0
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: user-service-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ASPNETCORE_URLS
          valueFrom:
            configMapKeyRef:
              name: user-service-config
              key: ASPNETCORE_URLS
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: user-service-db-secret
              key: ConnectionStrings__DefaultConnection
        livenessProbe:
          httpGet:
            path: /api/health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        # Stateless container - no persistent storage needed
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
