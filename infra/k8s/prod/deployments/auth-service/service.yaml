apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: prod
  labels:
    app: auth-service
    environment: production
    managed-by: freshharvest-market
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: auth-service
    environment: production