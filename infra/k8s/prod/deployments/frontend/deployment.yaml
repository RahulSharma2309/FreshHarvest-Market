# Frontend Deployment for Production
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: prod
  labels:
    app: frontend
    environment: production
    managed-by: freshharvest-market
spec:
  replicas: 3  # More replicas for production
  selector:
    matchLabels:
      app: frontend
      environment: production
  template:
    metadata:
      labels:
        app: frontend
        environment: production
        managed-by: freshharvest-market
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      # Frontend is a static SPA, doesn't need ServiceAccount access
      automountServiceAccountToken: false
      containers:
      - name: frontend
        image: ghcr.io/rahulsharma2309/freshharvest-market-frontend:v1.2.0
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        # Note: REACT_APP_API_BASE is typically set at build time
        # If your Dockerfile uses runtime env vars, uncomment below:
        # env:
        # - name: REACT_APP_API_BASE
        #   valueFrom:
        #     configMapKeyRef:
        #       name: frontend-config
        #       key: REACT_APP_API_BASE
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        # Stateless container - no persistent storage needed
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
