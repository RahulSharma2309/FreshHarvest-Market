<Project>
  <!-- Import from platform root .build folder -->
  <Import Project="$(MSBuildThisFileDirectory).build/src.props" Condition="Exists('$(MSBuildThisFileDirectory).build/src.props')" />
</Project>

