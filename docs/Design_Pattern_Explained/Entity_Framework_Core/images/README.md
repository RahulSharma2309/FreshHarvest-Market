# Images (optional)

Drop **your own** diagrams or scanned notes here if you like—the markdown series stands alone without them. Example filenames:

- `notebook-request-lifecycle.png`
- `notebook-ensurecreated.png`

Reference them from the parent docs with relative paths, e.g.:

```markdown
![Request lifecycle](../images/notebook-request-lifecycle.png)
```

Keeping images in-repo is optional; the markdown series is complete without them.
